---
description: iOS 自动调试与 UI 验证流程规则
globs: *.swift
---

# iOS 自动化调试与 UI 验证规范

当你修改了 iOS 项目中的 UI 代码、View 或业务逻辑后，请主动遵循以下“修改-验证-诊断”闭环流程：

## 1. 验证可用操作
调用 `list_actions` 工具，获取当前页面已注册的所有交互动作标识符（Action IDs）。

## 2. 模拟用户交互
如果修改涉及交互逻辑或页面跳转，调用 `trigger_action` 工具触发对应的 `action_id`，模拟用户点击或跳转行为。

## 3. 视觉与布局检查
调用 `get_screen` 工具获取 App 当前屏幕截图：
- 检查修改后的 UI 元素是否正常渲染。
- 检查是否存在布局重叠、截断或文字缺失问题。
- 确认视觉成果是否符合最初的需求描述。

## 4. 异常处理
- 如果 `list_actions` 或 `get_screen` 返回连接错误，提醒用户确认 App 是否已在运行且处于 DEBUG 模式。
- 如果 UI 渲染不符合预期，根据截图反馈自动调整 `.swift` 代码并再次验证。
